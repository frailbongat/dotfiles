---
description: Run the Design playbook, three variants per skill, I pick the winner
argument-hint: "[target path or brief]"
---

Run the Design playbook against: ${@:-the surface I describe next. Ask me for it before step 1.}

Read both of these in full first, in this order, before any other tool call:

1. `~/.pi/agent/pstack-bridge.md`
2. `~/.pi/agent/pstack/playbooks/design.md`

Then follow that playbook's numbered steps in order. Open the plan file the bridge's todolist rule names, with one `- [ ]` box per playbook step copied verbatim, before any task-specific box.

This playbook wins over every bundled poteto playbook for this task. Do not substitute Feature, Visual parity, Prototype, or figure-it-out for it, and do not compress the variant rounds into a single design.

Stop at step 6 and wait for my pick. Do not rank the variants for me.
